# node:sqlite is only unflagged from Node 23.4 and stable in 24. Pin 24 so no builder default
# silently lands on 22, where the import fails outright.
[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["echo 'no dependencies to install'"]

[start]
cmd = "node app/server.mjs"
